describe('File service and component', () => {

  it('Testing error from when accessing a bad file', () => {
    expect(1).toBe(2);
  });

  it('Testing the component has been called with the correct attributes', () => {
    expect(1).toBe(2);
  });

  it('Snapshot testing', () => {
    expect(1).toBe(2);
  });

  it('File service testing: massageExtras', () => {
    expect(1).toBe(2);
  });

  it('File service testing: calculateTotal', () => {
    expect(1).toBe(2);
  });

  it('File service testing: extractMetaDataFromFile', () => {
    expect(1).toBe(2);
  });

  it('File service testing: getBalanceMetaData', () => {
    expect(1).toBe(2);
  });

  it('File service testing: formatToCurrency', () => {
    expect(1).toBe(2);
  });

});
