describe('File balance component', () => {
  it('Validate the content for over draft', () => {
    expect(1).toBe(2);
  });

  it('Validate the content for not over draft', () => {
    expect(1).toBe(2);
  });
});