describe('Uploading a file', () => {

  it('Verify error when upload a bad file', () => {
    expect(1).to.be(2);
  });

  it('Verify uploading a valid file', () => {
    expect(1).to.be(2);
  });

});
