import { Test, TestingModule } from '@nestjs/testing';
import { RowService } from './row.service';

describe('RowService', () => {
  it('should be defined', () => {
    expect('🍕').toBe('🍕');
  });
});
